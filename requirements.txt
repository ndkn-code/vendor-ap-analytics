# Vendor & AP spend analytics — Python dependencies (generation + analysis)
numpy>=2.0
pandas>=2.2
matplotlib>=3.8
jupytext>=1.16   # round-trips notebook/analysis.py <-> analysis.ipynb
# Optional, only to execute the .ipynb interactively:
# jupyter
